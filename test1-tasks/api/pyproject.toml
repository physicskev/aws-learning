[project]
name = "test1-tasks"
version = "0.1.0"
description = "Task manager CRUD — experiment 1"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
