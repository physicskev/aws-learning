[project]
name = "test5-lambda-local"
version = "0.1.0"
description = "Local test server for Lambda handler — experiment 5"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
