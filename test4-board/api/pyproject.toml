[project]
name = "test4-board"
version = "0.1.0"
description = "Kanban board / ticket tracker — experiment 4"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
