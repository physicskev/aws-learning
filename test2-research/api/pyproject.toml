[project]
name = "test2-research"
version = "0.1.0"
description = "Claude-powered research viewer — experiment 2"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
