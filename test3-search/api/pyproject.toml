[project]
name = "test3-search"
version = "0.1.0"
description = "Full-text search explorer — experiment 3"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
